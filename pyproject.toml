[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "onex-telegram-control-center"
version = "1.3.1"
description = "ONEX Telegram Control Center"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["onex*"]
